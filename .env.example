# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_database_user
DB_PASSWORD=your_secure_database_password
DB_NAME=hospital_db

# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

JWT_SECRET="your_secure_jwt_secret_key_here"

# Email Service Configuration
EMAIL_SERVICE="gmail"
EMAIL_USER="your_email@gmail.com"
EMAIL_PASSWORD="your_app_specific_password"

# Frontend URL (for password reset links)
FRONTEND_URL="http://localhost:5173"

# Server Port
PORT=3001

# Rate Limiting (set to false to disable in development/testing)
RATE_LIMIT_ENABLED=true

# Email sending - set to false for testing without email service
EMAIL_ENABLED=true

# Security Settings
# Helmet security headers - set to false for debugging
HELMET_ENABLED=true

# Content Security Policy - set to false if CSP causes issues in development
CSP_ENABLED=true

# CORS origins - comma-separated list (defaults to localhost:5173,5174)
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Trust proxy - set to true when behind a load balancer/reverse proxy
TRUST_PROXY=false

# Node environment
NODE_ENV=development

# ═══════════════════════════════════════════════════════════════
# QUICK START GUIDE - How to Launch the Backend
# ═══════════════════════════════════════════════════════════════
#
# 1. INSTALL DEPENDENCIES
#    npm install
#
# 2. START DATABASE (Docker required)
#    docker compose up -d
#
# 3. RUN DATABASE MIGRATIONS
#    npx prisma migrate dev
#
# 4. SEED TEST DATA (Optional - for development/testing)
#    npm run db:seed
#
# 5. START THE SERVER
#    npm run dev          # Development with hot reload
#    npm start            # Production mode
#
# ═══════════════════════════════════════════════════════════════
# USEFUL COMMANDS
# ═══════════════════════════════════════════════════════════════
#
# Database Management:
#    npx prisma studio    # Visual database browser
#    npm run db:reset     # Reset DB and re-seed (WARNING: deletes all data)
#
# Testing:
#    npm run test:api     # Run comprehensive API tests
#                         # Requires: server running + seeded database
#
# For Testing (add to your .env):
#    RATE_LIMIT_ENABLED=false   # Disable rate limiting
#    EMAIL_ENABLED=false        # Disable email sending
#
# ═══════════════════════════════════════════════════════════════
